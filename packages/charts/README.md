# @axicharts/charts

Layer 1 React chart API for AxiCharts — `ChartContainer`, line/bar charts, industrial primitives, and `registerChartType` extension registry.

```bash
pnpm add @axicharts/charts @axicharts/charts-theme uplot
```

See the [monorepo README](https://github.com/Axidify/axicharts).
