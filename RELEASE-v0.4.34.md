# Release v0.4.34 — Optional design audit backlog

## Added

- **charts-echarts** — full pie `62%` radius @ compact bottom-legend mode; `resolveSunburstLayout`; swarm/ridgeline distribution compact axes
- **storybook** — `pie-status` + `cartesian-blocks` compare rows @ 360×280; `Audit/Studio` 3-way wall; Lane C **D-409–D-412**
- **docs** — Recharts reference index with Storybook IDs (**D-303**)

## Changed

- **docs** — pie **D-201** + cartesian blocks **D-105** → **Parity**; studio lane **D-310** → **Close**

## Packages

| Package | From | To |
|---------|------|-----|
| `@axicharts/charts` (+ platform siblings) | 0.4.33 | **0.4.34** |
| `@axicharts/charts-echarts` | 0.4.13 | **0.4.14** |
| `@axicharts/charts-planner` | 0.2.2 | **0.2.2** (peer `^0.4.34`) |

Triggered by GitHub release `v0.4.34` → [publish workflow](.github/workflows/publish.yml).
